// find a num if it is less or equal to zero
const lessOrEqualZero = (num) => {
    if (num<=0) {
        return num;
    } else {
        return ("not less than zero")
    }
}
console.log(lessOrEqualZero(-33)); 