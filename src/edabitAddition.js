// add a string of edabit to the user given value and store and show them in result

let name = "";

const edabitAddition = (a) => {
    let result = [];
    result.push(a+"EDABIT");
    return result;
}
console.log(edabitAddition('myself'));