// take the input and return greeting except for the luver ...cyber stalking

const luvToMubashir = (val) =>{
    if (val === "Mubashir") {
        return ("\'Hello! my love\'");
    } else {
        return ("\"Hello\"" + " " +val);
    }
}
console.log(luvToMubashir("Mubashir"));
console.log(luvToMubashir("checking"));